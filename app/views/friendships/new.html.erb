<h1>Create New Friendship</h1>

<%= form_with(model: @friendship) do |form| %>
  <% if @friendship.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@friendship.errors.count, "error") %> prohibited this Friendship from being saved:</h2>

      <ul>
        <% @friendship.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :member_id, "Select Member", style: "display: block" %>
    <%= form.select :member_id, options_from_collection_for_select(@members, :id, :name, @friendship.member_id),include_blank: true %>
  </div>
  <div>
    <%= form.label :friend_id, "Select Friend", style: "display: block" %>
    <%= form.select :friend_id, options_from_collection_for_select(@members, :id, :name, @friendship.friend_id), include_blank: true  %>
  </div>
  <div>
    <%= form.submit "Create Friendship" %>
  </div>
<% end %>

<%= link_to 'Back', :back %>